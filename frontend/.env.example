# Backend API base URL
# Leave empty in local dev to use Vite proxy and see API logs in terminal.
# Set full URL in production (for example: https://api.example.com)
VITE_API_BASE_URL=

# Optional local dev proxy target
VITE_PROXY_TARGET=http://127.0.0.1:8000
